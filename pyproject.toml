[tool.flet]
product = "버스캘린더"
dev_packages = { work-alarm = "extensions/work_alarm" }

[tool.flet.android]
dependencies = [
    "flet==0.86.4",
    "holidays>=0.65,<1",
    "korean-lunar-calendar>=0.4,<1",
    "work-alarm==0.1.0",
]

[tool.flet.flutter.pubspec.dependency_overrides]
jni = "1.0.3"
